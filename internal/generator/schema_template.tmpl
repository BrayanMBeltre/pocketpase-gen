// Code generated by pocketpase-gen. DO NOT EDIT.
package {{ .PackageName }}

{{ range .Collections }}
// {{ .StructName }} provides structured access to the {{ .CollectionName }} collection.
var {{ .StructName }} = struct {
    CollectionName string
    Fields struct {
        {{- range .Fields }}
        {{ .GoFieldName }} string
        {{- end }}
    }
}{
    CollectionName: "{{ .CollectionName }}",
    Fields: struct {
        {{- range .Fields }}
        {{ .GoFieldName }} string
        {{- end }}
    }{
        {{- range .Fields }}
        {{ .GoFieldName }}: "{{ .FieldName }}",
        {{- end }}
    },
}
{{ end }}


const (
{{- range $collection := .Collections -}}
    {{- range $field := $collection.Fields -}}
        {{- if $field.Options -}}
            {{- range $option := $field.Options }}
    {{ $collection.StructName | printf "%s" }}_{{ $field.FieldName | printf "%s" }}_{{ $option | printf "%s" }} = "{{ $option | printf "%s" }}"
            {{- end -}}
        {{- end -}}
    {{- end -}}
{{- end -}}
{{ "\n" }})
